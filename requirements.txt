# Python 3.9+
numpy>=1.20
scipy>=1.7
PyQt6>=6.5
pyqtgraph>=0.13
eulerangles>=1.0   # RELION <-> Dynamo Euler-angle conversion (only for --relion)
PyOpenGL>=3.1      # only for the in-app 3D view ("View 3D")
mrcfile>=1.4       # only for the 3D view's density overlay (.mrc maps)
